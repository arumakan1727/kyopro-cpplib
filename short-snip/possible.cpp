const auto possible = [](bool cond) { std::cout << (cond ? "possible" : "impossible") << '\n'; };
const auto Possible = [](bool cond) { std::cout << (cond ? "Possible" : "Impossible") << '\n'; };
const auto POSSIBLE = [](bool cond) { std::cout << (cond ? "POSSIBLE" : "IMPOSSIBLE") << '\n'; };
