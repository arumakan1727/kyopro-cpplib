const auto yesno = [](bool cond) { std::cout << (cond ? "yes" : "no") << '\n'; };
const auto YesNo = [](bool cond) { std::cout << (cond ? "Yes" : "No") << '\n'; };
const auto YESNO = [](bool cond) { std::cout << (cond ? "YES" : "NO") << '\n'; };
